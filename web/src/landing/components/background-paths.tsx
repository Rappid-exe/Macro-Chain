// Legacy re-export — the hero now uses the geometric shapes background
// from shape-landing-hero. This file is kept for backward compatibility.
export { BackgroundPaths } from "@/landing/components/ui/background-paths";
